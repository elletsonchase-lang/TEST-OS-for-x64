name: Build Custom Live OS

on: [push]

jobs:
  build-os:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Install System Compilation Tools
      run: |
        sudo apt-get update
        sudo apt-get install -y live-build squashfs-tools xorriso isolinux debootstrap debian-archive-keyring

    - name: Configure and Run Build Engine
      run: |
        # Wipe any bad layout memory
        lb clean --all || true
        
        # Build the configuration files explicitly setting the mirror paths
        lb config \
          --architectures amd64 \
          --distribution bookworm \
          --archive-areas "main contrib non-free non-free-firmware" \
          --mirror-bootstrap "http://debian.org" \
          --mirror-chroot "http://debian.org" \
          --mirror-binary "http://debian.org" \
          --binary-images iso-hybrid \
          --memtest none

        # Ensure hook file permissions carry over properly
        chmod +x config/hooks/live/01-setup-system.chroot

        # Trigger compilation using absolute mirrors
        sudo lb build
